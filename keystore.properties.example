# Copy this file to keystore.properties and fill in your private release signing values.
# Never commit keystore.properties or the .jks/.keystore file.

# Path is relative to the repository root unless an absolute path is used.
storeFile=release/number-addition-subtraction-upload-key.jks
storePassword=replace-with-private-store-password
keyAlias=number-addition-subtraction
keyPassword=replace-with-private-key-password

